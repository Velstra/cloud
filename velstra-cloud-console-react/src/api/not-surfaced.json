{
 "collect-medium": "Followed as a plain download link the console navigates to after `installMedium-nodes` answers \u2014 a fetch() would hold two gigabytes in memory to make a download of. Not a button.",
 "get-release-file": "What a node agent fetches when told what to run. A person has no use for a raw artefact in a browser; the medium for a person is `installMedium-nodes`."
}
